import axiosClient from "./axiosClient";

// const SchedulesApi = {
//     getSchedule(campus_id, days, user_code){
//         const url = `schedule/get-schedule?campus_id=${campus_id}&days=${days}&user_code=${user_code}`
//         return axiosClient.get(url)
//     },
//     getListAttendance(campus_id, term_id, user_code) {
//         const url = `schedule/get-list-attendance?campus_id=${campus_id}&term_id=${term_id}&user_code=${user_code}`
//         return axiosClient.get(url)
//     }    
   
// }
// export default SchedulesApi;